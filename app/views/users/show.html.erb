 <table>
    <% @history.each do |browsed_shopping_center| %>
     <tr>
       <td><%= link_to browsed_shopping_center.name, shopping_center_path(browsed_shopping_center) %></td>
      
     </tr>
    <% end %>
 </table>